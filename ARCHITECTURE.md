# DLNU-ACM 工作室官网 · 底层架构总纲 v1.0

> 本文档是整个网站**可持续开发性**的根基。它不描述"这一届做出了什么"，而是定义"任何一届都能在其上安全地新增功能而不破坏已有内容"的**契约**。
> 阅读顺序：本文档（总纲）→ `content/` 目录结构 → 各内容格式规范（`notes`/`announcements`/`awards`/`visualization-format`）→ `RUNNING.md`（交接手册）。

---

## 0. 设计目标（为什么这么设计）

这个网站必须满足三个持续性的硬约束，否则一届传一届就会崩：

1. **内容与代码分离**：改内容的人（管理员/下一届）不需要碰代码；改代码的人不需要重建全部内容。两者通过**稳定的内容格式**解耦。
2. **内容即资产**：所有内容（笔记、公告、证书、可视化、简介）都以**标准化的文件**存放在 `content/` 下，进出仓库 = 有版本历史 = 可交接、可回滚。**内容不应该只活在数据库里**，否则换人就丢。
3. **接口契约向后兼容，只增不减**：任何"新增功能"都通过**新增模块/新增版本号**进行，**不修改存量文件的格式**。已在 `visualization-format/RUN-CONTRACT.md` 确立这一原则，本文档把它推广到全站。

配套原则：
- **约定大于配置**：目录名、文件名、字段名都有固定约定，新一届靠 `README`/规范文档就能上手，不靠口口相传。
- **低运维**：有自有服务器，但运维要脚本化、文档化，服务崩了能自动拉起。
- **安全边界明确**：只有管理员能写内容；实验台小程序跑在沙箱里（见可视化规范）。

---

## 1. 技术栈总览（基于"有服务器 + 管理员懂编程"的既定前提）

| 层 | 选型 | 理由 |
|----|------|------|
| 前端框架 | **Next.js（全栈）**（或 Astro；推荐 Next.js 便于同进程做后台与 API） | 一个服务同时服务前台网站与 `/admin` 后台；有路由、SSR、成熟生态 |
| 语言 | **TypeScript** | 类型即文档，降低接手成本；内容格式也能借此约束 |
| 内容存储 | **`content/` 目录：Markdown + JSON + 图片**（核心决策） | 内容是文件 → 进 Git → 有历史、可回滚、可交接 |
| 附加数据 | **SQLite 起步**（可选，可迁 Postgres） | 存公告的已读统计、后台账号、访问量等"运行态"数据；单文件免运维 |
| 版本管理 | **Git**（校园 GitLab / GitHub） | 内容与代码一起版本化，是"一届传一届"的载体 |
| 进程守护 | **pm2 或 systemd** | 服务常驻、崩了自动拉起，避免"关终端服务就没" |
| 反向代理 | **Nginx 或 Caddy**（Caddy 更省，自动 HTTPS） | 443 对外、转发到 Node 进程 |
| 托管 | **自有服务器** | 已确认 |
| 后台框架 | **Next.js 内嵌 `/admin` 路由 + 轻量鉴权** | 后台与前台同一编译单元，少一套部署面 |

> **为什么内容用文件而不是纯数据库**：交接 = `git clone`，把仓库拉下来内容就全在；改坏能回滚；下一届看到的是**可读的 Markdown/JSON**，而不是一坨导出数据。运行态数据（浏览量等）才用数据库，它们是可丢弃的。

---

## 2. 目录结构（全站骨架）

```
DLNU-ACM算法学习/
├── ARCHITECTURE.md            # 本文档（架构总纲）
├── RUNNING.md                 # 交接手册：如何跑起来/如何部署/如何加模块
├── content/                   # ★ 内容即资产：唯一的数据来源（data）层
│   ├── intro.json             #   工作室简介（结构化）
│   ├── announcements/         #   公告（Markdown + front-matter）
│   ├── notes/                 #   算法笔记（Markdown + front-matter）
│   │   └── <topic>/           #     （可按专题分子目录）
│   ├── awards/                #   荣誉墙
│   │   ├── 2024-icpc.json     #     证书条目（结构化，引用图片）
│   │   └── images/            #     证书图片
│   └── visualization-format/  #   可视化/实验台（已建，见其 README）
│       ├── visualizations/    #     简易档：*.json
│       └── labs/              #     实验台档：<id>/
├── src/                       # 代码（logic）层：与 content 解耦
│   ├── app/                   #   Next.js 路由（含 /admin）
│   ├── components/            #   复用组件
│   ├── content/               #   读取 content/ 并把文件转成类型化数据的 loader
│   ├── lib/                   #   工具（markdown 渲染、schema 校验、鉴权）
│   └── types/                 #   TS 类型，与内容 schema 一一对应
├── schemas/                   # 内容格式的 JSON Schema（校验与"格式即规范"）
│   ├── note.schema.json
│   ├── announcement.schema.json
│   ├── award.schema.json
│   ├── intro.schema.json
│   └── (visualization-format 已内置自己的 schema)
├── scripts/                   # 运维/自动化脚本
└── .gitignore                 # 排除 node_modules、运行态数据库等
```

**核心分层**：`content/`（数据）完全独立于 `src/`（代码）。这就是可持续性的第一道关。

---

## 3. 内容格式规范（每类内容的契约）

每类内容有一套 **front-matter / JSON 字段 + 对应的 JSON Schema**。新增内容 = 按 Schema 写文件放进对应目录，**不改代码**。见各文件：

- `schemas/note.schema.json` — 笔记
- `schemas/announcement.schema.json` — 公告
- `schemas/award.schema.json` — 荣誉墙证书
- `schemas/intro.schema.json` — 工作室简介
- 可视化/实验台格式 → `content/visualization-format/`（已建）

> 这些 Schema 同时作为后台上传校验的依据，也是"格式即规范"——下一届看 Schema 就知道怎么写内容。

---

## 4. 四大核心功能的落地（在架构中的位置）

| 功能 | 数据层（content/） | 代码层（src/） | 后台（/admin） |
|------|------------------|--------------|----------------|
| 工作室简介 | `intro.json` | 简介页组件 | 编辑简介表单 |
| 笔记 | `notes/**/*.md`（含 front-matter） | 笔记列表/详情/分类/搜索页 | Markdown 编辑器（写入文件） |
| 公告 | `announcements/*.md` | 首页公告栏/列表/置顶/归档 | 公告发布表单 |
| 荣誉墙 | `awards/*.json` + images | 证书墙（按赛季/主办方分类） | 证书上传（图片+条目信息） |
| 可视化（两档） | `visualization-format/` | 播放页/实验台页面 | ZIP 上传 / JSON 上传 + 校验 |

后台统一做一件事：**把管理员的输入写成合规的 `content/` 文件**（并自动校验 + 可选 Git 提交）。这样"管理员上传"与"内容即文件、可版本化"两全。

---

## 5. 后台与权限

- 后台挂在 `/admin`（前台同一服务内），需**登录**。
- 权限模型：**管理员**（可写全部内容 + 管理成员）+ 成员只读前台。首版可简化为单一管理员账号（环境变量/单用户），再逐步扩展。
- 后台上传一律走**格式校验**（`schemas/*`），非法输入直接被拒，不会产生脏数据。
- 实验台上传走安全流程：解压 → manifest 校验 → 文件限制与扫描 → 静态托管（详见 `visualization-format`）。

---

## 6. 路由规划（首版）

```
/                 首页（简介 + 置顶公告 + 最新笔记 + 荣誉墙入口)
/notes            笔记列表（分类/标签/搜索）
/notes/[slug]     笔记详情
/announcements    公告列表
/awards           荣誉墙（按赛季/主办方/成员筛选）
/visualization    可视化列表（两档混合展示）
/visualization/[id]    简易档播放页
/lab/[id]         实验台页（沙箱 iframe + 参数面板）
/intro/about      工作室简介（或并入首页）
/admin/...        后台（笔记/公告/证书/可视化上传）
```

---

## 7. 可持续开发性的实施条款（本架构的核心承诺）

以下每条都是"一届传一届"的保障，写入 `RUNNING.md` 由每届遵守：

1. **新增内容 ≠ 改代码**：新增笔记/公告/证书/可视化，只往 `content/` 加合规文件即可。
2. **新增功能 ≠ 改核心**：新功能 = 新增一个路由/模块 + 新的内容类型（配套新 schema），不触碰已有模块的对外接口。
3. **契约只增不减，向后兼容**：格式版本号管理（已在可视化确立，推广到全站）；老文件在新站继续有效。
4. **所有内容进 Git**：内容改动 = 提交，形成历史；交接 = 拉仓库。
5. **类型化 loader**：`src/content/` 把 `content/` 读成 TS 类型，页面只面向类型，不直接碰文件——换格式时只需改 loader，页面代码几乎不动。
6. **Schema 即规范**：每类内容有 JSON Schema，后台用它校验，文档参照它解读。
7. **交接手册 + 脚本**：`README`/`RUNNING.md` 说明一切；`npm run` 提供 `dev/build/deploy`，部署脚本化，不靠"私房步骤"。

---

## 8. 首版本范围界定（赶在招新前）

**必做**：简介页、笔记（列表+详情+分类+搜索）、公告栏（置顶/归档）、荣誉墙（分类展示+大图）、可视化（简易档播放 + 实验台沙箱页 + 两个档的 Schema 校验后台上传）、单管理员登录、部署上线。

**后置（招新后迭代）**：成员登录/个人页、题库/做题记录、评论互动、访问量统计、更细的权限。

---

## 9. 已知边界与折衷（诚实声明）

- **实验台安全底线**：沙箱防"偷窃/破坏服务端"，但防不了"恶意脚本在访客浏览器搞事"，因此**信任仅限管理员**（见可视化规范）。
- **文件式内容 vs 后台**：后台上传本质 = "写文件"，所以管理员要有写动作（自动触发 Git 提交）；完全零 Git 的人可能觉得"保存后要提交"别扭 —— 首版可做成"后台保存即自动 commit"来隐掉这步。
- **SQLite 起步即可**：若日后并发/多人编辑升高，再迁 Postgres，不构成架构改版。

---

## 10. 演进路线图

1. **v0（本轮）**：架构总纲 + 内容 schema + 目录树（本文档及各 schema）。
2. **v1（首版，招新前）**：可运行的壳 + 四大功能最小闭环 + 后台上传 + 部署 + `RUNNING.md` 交接手册。
3. **v2+（每年迭代）**：新一届按第 7 节条款加功能；契约只增不减。

---

## 11. 待你确认

- **技术栈**：Next.js + TypeScript 是否认可（还是更想要 Astro / 别的）？
- **后台鉴权**：首版做"单管理员账号"是否够？还是要"管理员/成员"两级？
- **内容即文件 + 后台自动 commit** 这个结合方式你是否接受？
