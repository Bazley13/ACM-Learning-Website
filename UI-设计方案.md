# ACM 工作室官网 · UI 设计方案 v2.0

> 本文档是**界面与交互设计蓝图**。本版（v2）已**按此定稿落地**，配合了后台完整 CRUD 与首页三屏动画观感。
> 配套代码现状：Next.js + TypeScript 全栈，`content/` 文件式内容，可视化双档已建成。
> 本文档 v2 与实现一致；若你在某一界面看到与此冲突之处，以**已实现的分支/代码**为准，并回来更新本文档。

---

## 0. 设计语言与配色系统（你的核心诉求）

### 配色：ICPC 红黄蓝 + 竞赛语义色
- **主色系**：源自 ICPC 官方 logo 的红黄蓝三色，分布于边框、组件、动效、强调。
- **主调**：以蓝为主调，整体**明亮**，不晦暗。
- **不局限三色**，但以它们为主干；红黄蓝主要用在边框/组件/动效的搭配。
- **竞赛语义色**（工作室特有语言，全站通用，源自打比赛的直觉）：
  | 语义色 | 值 | 用在哪 |
  |--------|----|--------|
  | 绿 AC | `#22c55e` | 通过/正确/难度"入门"/实验台运行成功/荣誉金奖 |
  | 红 WA | `#ef4444` | 错误/报错/反馈问题 |
  | 橙·登峰 | `#f59e0b` | 难度「登峰」（v2：难度=入门/进阶/登峰） |
  | 黄 (highligh) | `#f59e0b` | 高亮/标注/强调/难度"进阶" |
  | 蓝 | `#3b82f6` | 主色：导航/链接/按钮/信息 |
  | 紫 (TLE) | `#a855f7` | 超时/特殊状态/装饰点缀 |
  | 灰蓝 | `#64748b` | 次级文本/边框/辅助 |

### VSCode 风格落地
- 代码块与数字标题用**等宽字体**（JetBrains Mono / Source Code Pro），代码高亮匹配上述四色系。
- 明亮浅色底（VSCode 浅色主题质感）：近白背景 + 浅灰蓝边框 + 卡片圆角。
- 卡片圆角中等（10-14px）、细边框、hover 提亮 + 色彩过渡。
- 左侧目录/大纲条用"活动项指示"动效。
- 资源推荐图标用**语义色圆标**（CSS 圆点，统一风格）；品牌 logo 场景用已装的图标库。

> 这套色系统一替换现有 `globals.css` 的 `:root` 变量即可全局生效，且与可视化"语义色令牌"天然一致（我会把两套令牌统一成一处：`lib/theme.ts` 单一来源）。

### 图标库（已安装，装于 D 盘项目 node_modules）
| 库 | 版本 | 完整包大小 | 用途 |
|----|------|-----------|------|
| lucide-react | 1.44.0 | ~33 MB | 线条图标（搜索/奖杯/箭头等），配 VSCode 质感 |
| react-icons | 5.7.0 | ~84 MB | 品牌 logo（资源推荐的 Codeforces/洛谷等） |

> 说明：磁盘占用是含全部图标的完整包，构建时 tree-shaking 只打包实际引用的几个，访客实际加载仅几十 KB。位置均在 `DLNU-ACM算法学习/node_modules`（D 盘），不占 C 盘。

---

## 1. 页面结构与导航

### 首页（三屏滚动，v2 定稿）
- **三屏纵向滚动吸附**（scroll-snap 逐屏切换，`Slide3D` 提供**立体卡片翻页感**：命中屏回正、其余后仰压暗）：
  - 屏 1 **Hero**：深色满底（WebGPU 可用则以 AeroShards 光栅背景，否则渐变兜底）+ 口号大标题 + 一句话简介 + 两个主按钮（了解工作室 / 体验实验台）+ 置顶公告栏 + 简要数据（笔记/实验台/荣誉数）。
  - 屏 2 **简介 + 公告**（居中单栏）。
  - 屏 3 **左侧 “ACM” 粒子字样（ParticleText）+ 右侧 照片轮播（比赛·日常）**。照片可在 `/admin/photos` 后台编辑。
- **屏 2 / 屏 3 背景**：蓝·黄·红三色动效光斑（缓慢漂移，`prefers-reduced-motion` 自动停用），整体明亮不过暗。
- 顶部导航为 **solid 顶栏**（`SiteNav`，上滑隐藏），不再是下拉 CardNav；左上角为 ICPC Logo。

### 导航结构（v2：solid 顶栏）
```
[LOGO]   [简介  公告  笔记  可视化实验台  资源推荐  荣誉墙  管理员（登录入口）]
```
- 各板块可有二级菜单（如"笔记"下放分类、"可视化"下放"简易演示/实验台/上传"）。
- 移动端折叠为汉堡菜单。

### 导航项路由（首版全落实）
| 板块 | 路由 | 说明 |
|------|------|------|
| 简介 | `/about` | 已建，配合新配色 |
| 公告 | `/announcements` | 已建 |
| 笔记 | `/notes` + `/notes/[slug]` | 重点改造（见第 2 节） |
| 可视化实验台 | `/visualization` + `/visualization/[id]` + `/lab/[id]` | 已建，视觉统一 |
| 资源推荐 | `/resources` | **新增板块** |
| 荣誉墙 | `/awards` | 已有，加 DriftWall + 弹窗 |
| 管理员 | `/admin` | 已建（登录/后台） |

---

## 2. 笔记模块（本次改造重点）

### 列表页 `/notes`
- **顶部搜索栏**：按笔记标题、算法名（slug）、tag 实时搜索（前端过滤）。
- **侧边筛选**：按算法类别（category）、难度筛选。
- **排序**：按 时间 / 点赞量。
- 笔记以**卡片列表**呈现（每卡：标题、分类徽章、难度徽章语义色、摘要、日期、作者、标签）。
- 点赞：首版**只做排序不改点赞功能**（已确认）。

### 详情页 `/notes/[slug]`（参考 Typora/说明浏览器排版）
- **左侧大纲/目录**：解析标题（h2/h3）生成锚点目录，走点击平滑滚动定位。**可折叠**（移动端默认收起，桌面常驻）——已确认。
- **正文渲染**：Typora 式清晰排版（标题层级、代码块高亮、表格、引用、图片）。
- **右侧/文末反馈按钮**：访客点击 → 标注"错误位置+说明" → 提交。
- 图片用相对路径（`images/xxx`），后端解析为可访问 URL。

### 笔记存储（沿用单 md + 图片目录）
```
content/notes/<分类>/<slug>.md
content/notes/<分类>/images/<图>.png
```
- 后台上传时：若上传 zip，则解压拆分出 md 到主文件 + images 到图片目录；相对路径自动改写。
- 现有可视化关联 `visualizationId` 保留。

---

## 3. 荣誉墙（DriftWall 展示 + lightbox 详情）

- **展示形态**：荣誉墙主体用 **DriftWall**（`resource/DriftWall.md`）——证书/奖杯图片排成 3D 透视"漂移墙"，整墙缓慢漂移动画，鼠标悬停某张抬起并恢复彩色，其余调暗。炫丽、与首版动画偏好一致。
- **点击交互**：点某张证书 → **居中 lightbox 弹窗**（已确认）：大图 + 获奖队伍 / 成员 / 比赛名称 / 赛事级别 / 赛季 / 备注。可关闭。
- **左右切换**：支持在 lightbox 内左右切换相邻证书（推荐，已确认）。
- 奖杯照片也支持（`images/` 放证书或奖杯图）。
- 卡片网格筛选（按比赛/赛季/级别）保留在首页下方，DriftWall 作为展示层。

---

## 4. 资源推荐（新增 `/resources`）

分类清单卡片式（图标用**语义色圆标** + 品牌 logo 用 react-icons）：
- **算法竞赛网站**（Codeforces / AtCoder / nowcoder / HDU / POJ 等）
- **算法学习网站**（OI-Wiki、洛谷、Usaco Guide 等）
- **视频/B站**（算法讲解视频/UP 主推荐）
- **工具**（可视化工具、刷题工具）
每类一组卡片：站名 + 简介 + 链接 + 语义色图标/品牌图标。

---

## 5. 后台 / 管理员

- 现有后台保留，补：**笔记成包上传**（zip 解压拆分 md+images）、**反馈查看与删除**（读 `content/feedback/`）、侧边栏也走新配色。
- 反馈数据落到 `content/feedback/`（JSON/md），管理员后台可浏览/标记已处理/删除。

---

## 6. 反馈机制

- 笔记详情页"反馈按钮" → 小表单（错误位置 + 说明，可选作者联系方式）→ 写入 `content/feedback/<slug>-<时间戳>.json`。
- 前台不显示反馈内容，仅管理员后台可见。
- 不强登录，防刷可加简单频率限制（localStorage / 时间戳）。

---

## 7. 动画组件引入策略（首版少量点缀）

首版只用少量，避免性能和维护负担：
- **首屏不使用粒子动画作背景**（已确认）：首屏大标题采用纯排版 + 轻量入场动效，保持官方、稳重。
- **ParticleText**（忠实移植自 ReactBits，见 `resource/ParticleText.md`）：v2 用于首页第三屏左侧 “ACM” 粒子字样（粒子散射→聚合成 ACM，辉光 + 缓慢漂浮 + 鼠标推开）。首屏保持纯排版 + 入场动效，不用粒子背景。密度/亮度/字号参数已在 `app/page.tsx` 调为既定观感。
- 笔记大纲侧边栏：可折叠（用 LineSidebar 或简化版）。
- 卡片 hover 动效（纯 CSS 过渡，不引动画库）。
- 其余动画组件（Galaxy、DotField、SplitFlapText 等）**预留目录**，后续迭代逐步启用。
  - DriftWall 例外：**首版就用于荣誉墙**（已确认）。

> 注：resource 里的组件是 ReactBits 的（依赖 gsap）。引入前确认许可与依赖体积；不用的暂不装。

---

## 8. 迭代分期建议

**首版（本次优化落地）**：
- 全局配色系统替换（`globals.css` 变量 + 语义色令牌统一为 `lib/theme.ts`）
- 下拉导航 + 首屏 Hero（纯排版入场动效，不使用粒子动画背景）
- 笔记列表改造（搜索/筛选/排序/卡片） + 详情页（可折叠大纲 + 反馈按钮 + Typora 排版）
- 荣誉墙 DriftWall + lightbox（含左右切换）
- 资源推荐板块（语义色圆标 + 品牌图标）
- 后台上传成包 + 反馈查看

**后续迭代**：其余动画组件逐步启用、点赞入库（如未来需要）、实验台增强、成员页。
